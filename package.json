{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "API_BASE=http://localhost:8080 pnpm --filter @workspace/web run dev",
    "start": "API_BASE=http://localhost:8080 pnpm --filter @workspace/web run start",
    "build:app": "node scripts/build-app.mjs",
    "build:api": "pnpm run typecheck && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/claude-design run build",
    "build": "pnpm run build:api && pnpm run build:app",
    "test:routing": "node scripts/smoke-routed-product.mjs",
    "test:routing:process": "node scripts/check-single-catalogue-server.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
